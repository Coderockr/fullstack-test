# Copy to .env and fill in your local values.
# For local development, run `npx prisma dev` — it starts a local Postgres
# and prints the DATABASE_URL to use (the generated .env already points to it).
DATABASE_URL="prisma+postgres://localhost:51213/?api_key=..."
PORT=3001
