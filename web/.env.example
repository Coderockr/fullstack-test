# SPA environment — only VITE_-prefixed vars are exposed to the client bundle.
# The single point of coupling to the backend: change this to point the SPA at
# any API deployment, no code changes required.
VITE_API_URL=http://localhost:8080/api
VITE_APP_NAME="Investment Manager"
